Do not write or modify any production code until the spec files in /spec are complete and internally consistent.

First output only:

A list of missing information or contradictions in the spec

A proposed repo file tree

An implementation plan in ordered steps

If any spec item is missing or ambiguous, stop and ask instead of guessing.

Only implement what is in spec/01_requirements.md.

Do not implement anything listed in spec/04_out_of_scope.md.

After the plan is approved by me, create changes as a pull request.
